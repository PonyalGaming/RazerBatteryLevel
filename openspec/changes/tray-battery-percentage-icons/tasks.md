## 1. Dependencies

- [x] 1.1 Add `resvg` (with its bundled `usvg` and `tiny-skia`) to `Cargo.toml` dependencies
- [x] 1.2 Verify the project still builds — installed Rust via winget; `cargo build --release` succeeded in 1m26s
- [x] 1.3 Confirm whether `image` is still needed elsewhere; note it for later removal if unused — only used by the two replaced icon fns, so removed from `Cargo.toml`

## 2. SVG icon assets

- [x] 2.1 Design one shared SVG template: viewBox 32×32, centered number drawn as font-free vector geometry (seven-segment `<rect>`s), with color placeholder token `__FG__`
- [x] 2.2 Ensure the template auto-fits width for 1-, 2-, and 3-digit values (special-case `100`)
- [x] 2.3 Write a committed generator (`tools/generate_icons.py`) that stamps values `0`–`100` into the template and writes `assets/icons/<NNN>.svg`
- [x] 2.4 Run the generator and produce all icons (`assets/icons/000.svg` … `100.svg`, 3-digit zero-padded for stable ordering)
- [x] 2.5 Add the special-state assets: `0%` (000.svg) and unknown/searching placeholder (`unknown.svg`); charging handled as a green `__FG__` color at render time
- [x] 2.6 Sanity-check icon geometry (segment layout verified for 4/2/…); pixel-level render check deferred to a machine with a rasterizer

## 3. Runtime SVG rendering

- [x] 3.1 Create the icon rendering module (`src/icon.rs`) that maps a battery level to the correct embedded SVG string
- [x] 3.2 Embed the SVG assets at compile time (`src/icon_data.rs` with `include_str!`, generated by the script) so the binary is self-contained
- [x] 3.3 Implement color-token substitution based on threshold (normal / low ≤ `BATTERY_LOW_LEVEL` / critical ≤ `BATTERY_CRITICAL_LEVEL`) and charging state
- [x] 3.4 Implement `render_battery_icon(level, is_charging, critical, low)`: parse SVG with `usvg`, render to `tiny_skia::Pixmap`, convert to RGBA, build `tray_icon::Icon::from_rgba`
- [x] 3.5 Handle premultiplied-alpha conversion (`unpremultiply`) to avoid edge fringing
- [x] 3.6 Handle unknown level (`-1`) → searching placeholder, and clamp levels to `0`–`100`

## 4. Wire into the tray

- [x] 4.1 Replace `TrayApp::create_icon` startup icon with `icon::render_unknown_icon()`
- [x] 4.2 Replace `TrayApp::get_battery_icon` usage in `update()` with `icon::render_battery_icon(...)`
- [x] 4.3 Keep the existing "only update on change" guard (`old_battery_level != battery_level || charging changed`)
- [x] 4.4 Leave tooltip, notifications, menu, and device polling unchanged; removed now-dead PNG-loading code and the `image` dependency
- [x] 4.5 Add `mod icon;` and `mod icon_data;` to `main.rs`

## 5. Verify

- [x] 5.1 `cargo build --release` succeeds — only warning is a benign linker `.lib/.exp` note, none about icon code; exe is 3.39 MB
- [ ] 5.2 Run the app and confirm the tray shows the numeric percentage, updates on change, and matches the tooltip — NEEDS: a connected Razer device + visual check on your machine
- [ ] 5.3 Confirm color changes at low (≤15) and critical (≤5) thresholds and that charging shows a distinct appearance — NEEDS: device + visual check
- [x] 5.4 Confirm the searching placeholder renders at startup — smoke-tested: app launched, ran 6s, startup SVG rendered without panic (visual confirm of the dots still worth a glance)
- [x] 5.5 Update `README.md` (description and the Todo list) to reflect numeric percentage icons
